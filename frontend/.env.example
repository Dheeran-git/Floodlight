# Floodlight Frontend Environment Variables
# Copy to .env.local and fill in values. All vars must be prefixed with VITE_.

# Mapbox GL JS access token. Without it, the crisis map shows a placeholder.
VITE_MAPBOX_TOKEN=
